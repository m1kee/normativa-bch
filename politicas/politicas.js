let elementsPerPage = 10;

let politicGrid = {
    total: 0,
    elements: []
};

let rulesGrid = {
    total: 0,
    elements: []
};

let proceduresGrid = {
    total: 0,
    elements: []
};

window.onload = () => {
    const params = new URLSearchParams(window.location.search);
};